import '@/styles/globals.css'

const Home = ({ }) => {
    return (
        <main className={`flex flex-col justify-center h-screen`}>
            <h1>Things Forum</h1>
        </main>
    )
}

export default Home;