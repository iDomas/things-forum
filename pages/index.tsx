import '@/styles/globals.css'

const Home = ({ }) => {
    return (
        <main className={`flex flex-col justify-center h-full`}>
            <h1 className="text-4xl font-bold tracking-tight">Things Forum</h1>
        </main>
    )
}

export default Home;